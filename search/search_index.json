{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"oa-configurator","text":"<p>A shared configuration layer for the OMOP-oriented Python stack.</p> <p>oa-configurator gives OMOP tools a single typed configuration file instead of a tangle of environment variables and package-local <code>.env</code> files.</p>"},{"location":"#key-concepts","title":"Key Concepts","text":"<ul> <li>Connection: A concrete database endpoint (host, dialect, credentials)</li> <li>Resource:  A logical role bundle, e.g. primary OMOP CDM DB, embedding DB, artficat paths</li> <li>Profile: A named environment (e.g. <code>local</code>, <code>prod</code>) that patches resources and tools</li> <li>Tool:  Per-tool defaults, e.g. backend, default resource, storage roots</li> <li>Logging: One call configures consistent log output for the entire OMOP Python stack</li> </ul> <p>Info</p> <p>Configuration lives in one TOML file (default <code>~/.config/omop/config.toml</code>, overridable via <code>OA_CONFIG_PATH</code>) and is loaded once. The Resolver turns logical names into typed, credential-resolved handles ready for use.</p>"},{"location":"#quick-example","title":"Quick Example","text":"From a config fileInline (no file)Session override <pre><code>from oa_configurator import load_stack_config, Resolver\n\nconfig = load_stack_config()                        # reads CONFIG_PATH (default ~/.config/omop/config.toml)\nresolver = Resolver(config)\n\nresource = resolver.resolve_resource(\"default\")\nengine   = resource.create_engine()                # SQLAlchemy Engine, schema_translate_map applied\n</code></pre> <pre><code>from oa_configurator import StackConfig, DatabaseConfig, ResourceConfig, Resolver\n\nconfig = StackConfig.for_session(\n    databases={\"local\": DatabaseConfig(dialect=\"postgresql\", host=\"localhost\",\n                                       database_name=\"omop\", password=\"omop\")},\n    resources={\"default\": ResourceConfig(database=\"local\", cdm_schema=\"cdm\")},\n)\nengine = Resolver(config).resolve_resource(\"default\").create_engine()\n</code></pre> <pre><code>from oa_configurator import load_stack_config, DatabaseConfig, ResourceConfig, Resolver\n\n# Load shared team config, redirect one resource to a local SQLite database\nengine = (\n    Resolver(load_stack_config())\n    .with_overrides(\n        databases={\"local\": DatabaseConfig(dialect=\"sqlite\", database_name=\"/data/local.db\")},\n        resources={\"default\": ResourceConfig(database=\"local\", cdm_schema=\"omop\")},\n    )\n    .resolve_resource(\"default\")\n    .create_engine()\n)\n</code></pre>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Quick Start: install and get a working engine in minutes</li> <li>Config File Reference: every TOML field documented</li> <li>Logging: consistent log output across the entire OMOP stack</li> <li>Inline &amp; Session Usage: construct config in code without a file</li> <li>Profiles &amp; Overlays: switch between environments cleanly</li> <li>Integration: add <code>omop-config configure</code> support to your package</li> </ul>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#purpose","title":"Purpose","text":"<p><code>oa-configurator</code> is a shared configuration layer for the OMOP-oriented Python stack:</p> <ul> <li><code>omop-alchemy</code></li> <li><code>orm-loader</code></li> <li><code>omop-emb</code></li> <li><code>omop-graph</code></li> <li><code>omop-spires</code></li> </ul> <p>It replaces per-package <code>.env</code> files, inconsistent env var lookups, and duplicated engine-creation boilerplate with a single typed TOML file and a common resolver interface.</p>"},{"location":"architecture/#core-concepts","title":"Core concepts","text":""},{"location":"architecture/#database","title":"Database","text":"<p>A concrete database endpoint: dialect, host, credentials, database name. Stored in <code>[databases.&lt;name&gt;]</code>.</p>"},{"location":"architecture/#resource","title":"Resource","text":"<p>A logical role bundle that maps OMOP CDM roles to databases and schema names:</p> <ul> <li><code>database</code>: the CDM server database</li> <li><code>vocab_database</code>: optional separate database for vocabulary (falls back to <code>database</code>)</li> <li><code>cdm_schema</code>: schema where CDM clinical tables live (required)</li> <li><code>vocab_schema</code> (optional): falls back to <code>cdm_schema</code></li> <li><code>results_schema</code> (optional): Achilles/Atlas results</li> </ul>"},{"location":"architecture/#tool","title":"Tool","text":"<p>Per-package configuration in <code>[tools.&lt;name&gt;]</code>. The core model stores only <code>default_resource</code> and an <code>extra</code> dict. Each consuming package defines a typed <code>PackageConfigBase</code> subclass that provides a typed view over <code>extra</code>.</p>"},{"location":"architecture/#profile","title":"Profile","text":"<p>A named overlay (<code>[profiles.&lt;name&gt;]</code>) that replaces specific connections, resources, or tools when active. Full model replacement, not a partial patch. Activate via <code>omop-config use &lt;profile&gt;</code> (persists to TOML) or <code>OA_ACTIVE_PROFILE=&lt;profile&gt;</code> (per-session).</p>"},{"location":"architecture/#data-flow","title":"Data flow","text":"<pre><code>~/.config/omop/config.toml\n         \u2502\n         \u25bc\n    load_stack_config()\n         \u2502  reads TOML \u2192 StackConfig\n         \u2502  applies active profile overlay\n         \u25bc\n       Resolver\n         \u2502\n         \u251c\u2500 resolve_database(name)   \u2192 ResolvedDatabaseTarget\n         \u2502                               .url  (plaintext, internal)\n         \u2502                               .safe_url  (redacted, for logs)\n         \u2502                               .create_engine()\n         \u2502\n         \u251c\u2500 resolve_resource(name)   \u2192 ResolvedResource\n         \u2502                               .database / .vocab_database  (ResolvedDatabaseTarget)\n         \u2502                               .cdm_schema / .vocab_schema / .results_schema\n         \u2502                               .schema_translate_map()\n         \u2502                               .create_engine(role=\"primary\"|\"vocab\")\n         \u2502\n         \u2514\u2500 resolve_tool(name)       \u2192 ResolvedToolConfig\n                                         .extra  (raw dict; typed by PackageConfigBase.from_stack())\n</code></pre>"},{"location":"architecture/#package-integration-via-entry-points","title":"Package integration via entry points","text":"<p>Consuming packages subclass <code>PackageConfigBase</code> and register via a <code>pyproject.toml</code> entry point:</p> <pre><code>[project.entry-points.\"omop.config\"]\nmy_package = \"my_package.config:MyPackageConfig\"\n</code></pre> <p><code>omop-config configure my_package</code> discovers the class at runtime via <code>importlib.metadata.entry_points(group=\"omop.config\")</code>, presents the typed fields for interactive configuration, and writes the result to <code>[tools.my_package.extra]</code>. <code>oa-configurator</code> itself has no knowledge of any consuming package.</p>"},{"location":"architecture/#schema-translate-map","title":"Schema translate map","text":"<p><code>ResolvedResource.schema_translate_map()</code> returns the SQLAlchemy-compatible schema translate dict:</p> <pre><code>{None: \"omop\", \"vocab\": \"omop_vocab\", \"results\": \"results\"}\n</code></pre> <p>OMOP ORM models (omop-alchemy) carry <code>schema=None</code> or <code>schema=\"vocab\"</code> on their <code>__table_args__</code>. The translate map routes them to the correct schema at runtime without changing model definitions.</p>"},{"location":"architecture/#security","title":"Security","text":"<p>Passwords are stored in plaintext in <code>~/.config/omop/config.toml</code>. Restrict permissions:</p> <pre><code>chmod 600 ~/.config/omop/config.toml\n</code></pre> <p><code>ResolvedDatabaseTarget.safe_url</code> and <code>ResolvedDatabaseTarget.url</code> are distinct: <code>safe_url</code> has the password replaced with <code>***</code> and is used for all logging and display. The <code>.url</code> value (with plaintext password) is used only for engine creation and never logged by the library.</p> <p><code>RedactingFormatter</code> (applied by all non-library log presets) scrubs both <code>key=value</code> patterns and <code>://user:password@host</code> URL patterns from log output.</p> <p>Future work: <code>secret_source</code> support (<code>env:VAR</code>, <code>file:path</code>, Vault, cloud secret managers) is planned but not implemented in this version.</p>"},{"location":"architecture/#config-path","title":"Config path","text":"<p>Default: <code>~/.config/omop/config.toml</code>. Override with <code>OA_CONFIG_PATH=&lt;path/to/config.toml&gt;</code> (must end in <code>.toml</code>; <code>~</code> is expanded). Resolved once at module load time and stored as <code>CONFIG_PATH</code>. Use <code>OA_ACTIVE_PROFILE</code> to switch profiles within a file without changing the path.</p>"},{"location":"architecture/#future-work","title":"Future work","text":"<ul> <li><code>secret_source</code> on <code>DatabaseConfig</code>: <code>env:VARNAME</code>, <code>file:PATH</code>, Vault, cloud secret managers</li> <li>Async engine factory (<code>ResolvedDatabaseTarget.create_async_engine()</code>)</li> <li>Project-local overlay (<code>./oa-config.toml</code>) layered over user config</li> </ul>"},{"location":"config-reference/","title":"Config Reference","text":"<p>Note</p> <p>Configuration lives at <code>~/.config/omop/config.toml</code> by default. Override the path by setting <code>OA_CONFIG_PATH</code> to any <code>.toml</code> file (e.g. <code>OA_CONFIG_PATH=~/projects/omop.toml</code>). Use <code>OA_ACTIVE_PROFILE</code> to switch profiles within a file at runtime.</p>"},{"location":"config-reference/#top-level-fields","title":"Top-level fields","text":"Field Type Default Description <code>active_profile</code> string <code>null</code> Name of the profile to activate. Can be overridden by the <code>OA_ACTIVE_PROFILE</code> env var."},{"location":"config-reference/#databasesname","title":"<code>[databases.&lt;name&gt;]</code>","text":"<p>One section per named database endpoint. The name is referenced by resources and profiles.</p> Field Type Required Description <code>dialect</code> string yes SQLAlchemy dialect string, e.g. <code>postgresql+psycopg</code>, <code>mssql+pyodbc</code>, <code>sqlite</code> <code>host</code> string no Hostname or IP <code>port</code> int no Port number <code>user</code> string no Database username <code>password</code> string no Plaintext password (see security note below) <code>database_name</code> string no Database name on the server. For SQLite, use <code>:memory:</code> or an absolute path. <code>read_only</code> bool <code>false</code> Hint only; enforcement depends on the dialect <p>Security note</p> <p>Passwords are stored in plaintext in this file. Restrict permissions with <code>chmod 600 ~/.config/omop/config.toml</code>. Secret-management support (env-backed passwords, Vault, etc.) is planned for a future release.</p>"},{"location":"config-reference/#example-postgresql","title":"Example: PostgreSQL","text":"<pre><code>[databases.cdm]\ndialect       = \"postgresql+psycopg\"\nhost          = \"localhost\"\nport          = 5432\nuser          = \"omop\"\npassword      = \"changeme\"\ndatabase_name = \"omop_cdm\"\n</code></pre>"},{"location":"config-reference/#example-sqlite-in-memory-for-tests","title":"Example: SQLite in-memory (for tests)","text":"<pre><code>[databases.test_db]\ndialect       = \"sqlite\"\ndatabase_name = \":memory:\"\n</code></pre>"},{"location":"config-reference/#resourcesname","title":"<code>[resources.&lt;name&gt;]</code>","text":"<p>A resource maps logical OMOP CDM roles to named connections and schema names.</p> Field Type Required Description <code>database</code> string yes Database name (from <code>[databases.*]</code>) for the CDM server <code>vocab_database</code> string no Separate database if vocabulary lives on a different server. Falls back to <code>database</code>. <code>cdm_schema</code> string yes Schema where CDM clinical tables live <code>vocab_schema</code> string no Vocabulary schema. Falls back to <code>cdm_schema</code> when not set. <code>results_schema</code> string no Achilles / Atlas results schema"},{"location":"config-reference/#example-all-in-one-schema","title":"Example: all in one schema","text":"<pre><code>[resources.default]\ndatabase   = \"cdm\"\ncdm_schema = \"omop\"\n</code></pre>"},{"location":"config-reference/#example-separate-vocab-and-results-schemas","title":"Example: separate vocab and results schemas","text":"<pre><code>[resources.default]\ndatabase       = \"cdm\"\ncdm_schema     = \"omop\"\nvocab_schema   = \"omop_vocab\"\nresults_schema = \"results\"\n</code></pre>"},{"location":"config-reference/#example-vocabulary-on-a-separate-server","title":"Example vocabulary on a separate server","text":"<pre><code>[resources.default]\ndatabase      = \"cdm\"\nvocab_database = \"central_vocab\"\ncdm_schema    = \"omop\"\n</code></pre>"},{"location":"config-reference/#toolsname","title":"<code>[tools.&lt;name&gt;]</code>","text":"<p>Per-package configuration. The <code>name</code> must match the package's <code>tool_name</code> class variable on its <code>PackageConfigBase</code> subclass.</p> Field Type Default Description <code>default_resource</code> string <code>null</code> Resource name this tool uses when none is specified <code>extra</code> table <code>{}</code> Package-specific key/value pairs. Each package defines its own typed fields that map here."},{"location":"config-reference/#example-omop_emb","title":"Example: omop_emb","text":"<pre><code>[tools.omop_emb]\ndefault_resource = \"default\"\n\n[tools.omop_emb.extra]\nbackend             = \"sqlitevec\"\nembedding_file_root = \"/data/embeddings\"\n</code></pre>"},{"location":"config-reference/#profilesname","title":"<code>[profiles.&lt;name&gt;]</code>","text":"<p>A profile overlays connections, resources, and tools over the base config when active.</p> Field Type Description <code>databases</code> table Database configs that replace base databases with the same name, or add new ones <code>resources</code> table Resource configs that replace base resources with the same name, or add new ones <code>tools</code> table Tool configs that replace base tools with the same name, or add new ones <p>Profile entries use the same field definitions as the base sections above.</p> <p>Example test profile pointing to a local test database:</p> <pre><code>[profiles.test.databases.cdm]\ndialect       = \"postgresql+psycopg\"\nhost          = \"localhost\"\nport          = 5433\nuser          = \"test_user\"\npassword      = \"test_pass\"\ndatabase_name = \"omop_test\"\n\n[profiles.test.resources.default]\ndatabase   = \"cdm\"\ncdm_schema = \"test_omop\"\n</code></pre> <p>Activate it: <code>omop-config use test</code> or <code>OA_ACTIVE_PROFILE=test</code>.</p>"},{"location":"config-reference/#logging","title":"<code>[logging]</code>","text":"<p>Controls log output for <code>oa_configurator</code> and any consuming packages. Defaults to library preset (WARNING, no handler).</p> Field Type Default Description <code>preset</code> string <code>\"library\"</code> One of <code>library</code>, <code>notebook</code>, <code>application</code>, <code>production</code> <code>level</code> string <code>null</code> Override the preset's level: <code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>, <code>CRITICAL</code> <code>loggers</code> table <code>{}</code> Fine-grained level overrides for specific loggers, e.g. <code>{\"sqlalchemy.engine\": \"INFO\"}</code> <p>See Logging for full details.</p>"},{"location":"inline-usage/","title":"Inline &amp; Session Usage","text":"<p><code>oa-configurator</code> works without a TOML file. Two patterns cover the main use cases.</p>"},{"location":"inline-usage/#stackconfigfor_session-pure-inline-construction","title":"<code>StackConfig.for_session()</code>: pure inline construction","text":"<p>Equivalent to loading a TOML file, but the config is built in code. Useful for:</p> <ul> <li>Notebooks and quick scripts where writing a file is overhead</li> <li>Tests that need an isolated, reproducible config</li> <li>Programmatic config generation (e.g. CI pipelines)</li> </ul> <pre><code>from oa_configurator import StackConfig, DatabaseConfig, ResourceConfig, Resolver\n\nconfig = StackConfig.for_session(\n    databases={\n        \"local\": DatabaseConfig(\n            dialect=\"postgresql+psycopg\",\n            host=\"localhost\",\n            database_name=\"omop\",\n            user=\"omop\",\n            password=\"omop\",\n        )\n    },\n    resources={\n        \"default\": ResourceConfig(\n            database=\"local\",\n            cdm_schema=\"cdm\",\n            vocab_schema=\"vocab\",\n        )\n    },\n)\nengine = Resolver(config).resolve_resource(\"default\").create_engine()\n</code></pre>"},{"location":"inline-usage/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>databases</code> dict | None <code>{}</code> Named <code>DatabaseConfig</code> objects or raw dicts <code>resources</code> dict | None <code>{}</code> Named <code>ResourceConfig</code> objects or raw dicts <code>tools</code> dict | None <code>{}</code> Named <code>ToolConfig</code> objects or raw dicts <code>profiles</code> dict | None <code>{}</code> Named <code>ProfileOverrideConfig</code> objects or raw dicts <code>active_profile</code> str | None <code>None</code> Profile to activate"},{"location":"inline-usage/#validation","title":"Validation","text":"<p>Cross-references are validated at construction time, same as for file-loaded configs. A resource referencing an unknown connection raises immediately:</p> <pre><code>StackConfig.for_session(\n    databases={\"local\": DatabaseConfig(dialect=\"sqlite\", database_name=\":memory:\")},\n    resources={\"default\": ResourceConfig(database=\"typo\", cdm_schema=\"omop\")},  # raises ValueError\n)\n</code></pre>"},{"location":"inline-usage/#in-tests","title":"In tests","text":"<p><code>for_session()</code> is the recommended pattern for package tests. No file I/O, fully isolated:</p> <pre><code>from oa_configurator import StackConfig, Resolver\n\ndef test_something():\n    cfg = StackConfig.for_session(\n        databases={\"db\": {\"dialect\": \"sqlite\", \"database_name\": \":memory:\"}},\n        resources={\"default\": {\"database\": \"db\", \"cdm_schema\": \"omop\"}},\n        tools={\"my_package\": {\"extra\": {\"backend\": \"test_backend\"}}},\n    )\n    resolver = Resolver(cfg)\n    engine = resolver.resolve_resource(\"default\").create_engine()\n    # ...\n</code></pre>"},{"location":"inline-usage/#resolverwith_overrides-session-level-override","title":"<code>Resolver.with_overrides()</code>: session-level override","text":"<p>Loads the shared config file, then replaces specific connections or resources for this session without touching the file. Useful for:</p> <ul> <li>Tests that swap prod connections for in-memory equivalents</li> <li>Notebook sessions that redirect one resource to a local database</li> <li>Sharing a team config but running with personal credentials locally</li> </ul> <pre><code>from oa_configurator import load_stack_config, DatabaseConfig, ResourceConfig, Resolver\n\nengine = (\n    Resolver(load_stack_config())\n    .with_overrides(\n        databases={\n            \"local\": DatabaseConfig(dialect=\"sqlite\", database_name=\":memory:\")\n        },\n        resources={\n            \"default\": ResourceConfig(database=\"local\", cdm_schema=\"omop\")\n        },\n    )\n    .resolve_resource(\"default\")\n    .create_engine()\n)\n</code></pre>"},{"location":"inline-usage/#parameters_1","title":"Parameters","text":"Parameter Type Description <code>connections</code> dict | None Entries merged over the existing connections (new keys added; existing keys replaced) <code>resources</code> dict | None Entries merged over the existing resources <code>tools</code> dict | None Entries merged over the existing tools"},{"location":"inline-usage/#what-is-preserved","title":"What is preserved","text":"<ul> <li>The original active profile</li> <li>All profiles and their overlays</li> <li>All connections, resources, and tools not mentioned in the overrides</li> </ul>"},{"location":"inline-usage/#what-is-validated","title":"What is validated","text":"<p>Cross-references are checked against the merged result. A resource override that references a connection that exists in neither the original config nor the override dict raises <code>ValueError</code> at call time.</p> <pre><code>Resolver(load_stack_config()).with_overrides(\n    resources={\"default\": ResourceConfig(database=\"nonexistent\", cdm_schema=\"omop\")}  # raises\n)\n</code></pre>"},{"location":"inline-usage/#comparison","title":"Comparison","text":"<code>for_session()</code> <code>with_overrides()</code> Needs a config file No Yes Inherits shared team config No Yes Profile overlays preserved Only if explicitly passed Yes Primary use case Tests, scripts, CI Notebooks, per-user local redirects"},{"location":"integration/","title":"Integration","text":"<p>Info</p> <p>This guide showcases how to make your package support <code>omop-config configure &lt;package&gt;</code> and use <code>oa-configurator</code> for engine creation and logging.</p>"},{"location":"integration/#overview","title":"Overview","text":"<p>Each package that integrates with <code>oa-configurator</code>:</p> <ol> <li>Subclasses <code>PackageConfigBase</code> with its typed config fields</li> <li>Registers the class via an entry point in <code>pyproject.toml</code></li> <li>Calls <code>MyPackageConfig.get_config()</code> to read its config</li> <li>Uses <code>Resolver.from_active_config().resolve_resource(\"default\").create_engine()</code> for SQLAlchemy</li> <li>Calls <code>configure_logging(verbosity=verbose, extra_namespaces=[\"&lt;package&gt;\"])</code> at startup</li> </ol>"},{"location":"integration/#steps-for-integration","title":"Steps for integration","text":""},{"location":"integration/#1-add-the-dependency","title":"1: Add the dependency","text":"<p>In your <code>pyproject.toml</code>:</p> <pre><code>[project.dependencies]\n\"oa-configurator&gt;=0.2.0\"  # version may vary\n</code></pre>"},{"location":"integration/#2-define-your-config-class","title":"2. Define your config class","text":"<p>In <code>src/&lt;package&gt;/config.py</code>:</p> <pre><code>from typing import ClassVar\nfrom pydantic import Field\nfrom oa_configurator import PackageConfigBase\n\n\nclass MyPackageConfig(PackageConfigBase):\n    tool_name: ClassVar[str] = \"my_package\"   # maps to [tools.my_package] in TOML\n\n    # Declare typed fields; they're backed by ToolConfig.extra in the TOML\n    backend: str = Field(default=\"default\", description=\"Backend to use.\")\n    data_path: str | None = Field(default=None, description=\"Path to local data files.\")\n</code></pre> <p><code>get_config()</code> is inherited from <code>PackageConfigBase</code> \u2014 call <code>MyPackageConfig.get_config()</code> to load from the active stack config. <code>from_stack()</code> reads the <code>[tools.my_package.extra]</code> section and validates it against your typed fields. If the section is missing, fields fall back to their defaults.</p>"},{"location":"integration/#3-register-the-entry-point","title":"3. Register the entry point","text":"<pre><code>[project.entry-points.\"omop.config\"]\nmy_package = \"my_package.config:MyPackageConfig\"\n</code></pre> <p>After installing your package, <code>omop-config configure my_package</code> will find and prompt through your fields.</p>"},{"location":"integration/#4-engine-creation","title":"4. Engine creation","text":"<pre><code>from oa_configurator import Resolver\n\nengine = Resolver.from_active_config().resolve_resource(\"default\").create_engine()\n</code></pre> <p><code>create_engine()</code> applies the <code>schema_translate_map</code> automatically so OMOP ORM models route to the right schemas without changes.</p> <p>For the vocabulary database:</p> <pre><code>vocab_engine = Resolver.from_active_config().resolve_resource(\"default\").create_engine(role=\"vocab\")\n</code></pre>"},{"location":"integration/#5-logging","title":"5. Logging","text":"<p>At your package's CLI entry point or startup:</p> <pre><code>from oa_configurator import configure_logging\n\n# verbosity comes from the -v/-vv CLI flag count\nconfigure_logging(verbosity=verbose, extra_namespaces=[\"my_package\"])\n</code></pre> <p>Pass <code>load_stack_config()</code> as the first argument instead of <code>verbosity=</code> to use the <code>[logging]</code> block from the config file:</p> <pre><code>configure_logging(load_stack_config(), verbosity=verbose, extra_namespaces=[\"my_package\"])\n</code></pre>"},{"location":"integration/#testing","title":"Testing","text":"<p>Tests fall into two tiers with different requirements.</p>"},{"location":"integration/#unit-tests-stackconfigfor_session","title":"Unit tests: <code>StackConfig.for_session()</code>","text":"<p>Unit and mock-based tests must never touch <code>~/.config/omop/config.toml</code>. Use <code>StackConfig.for_session()</code> with <code>monkeypatch</code> to inject a fully in-memory config:</p> <pre><code>from oa_configurator import StackConfig, Resolver\n\ndef test_something(monkeypatch):\n    cfg = StackConfig.for_session(\n        databases={\"db\": {\"dialect\": \"sqlite\", \"database_name\": \":memory:\"}},\n        resources={\"default\": {\"database\": \"db\", \"cdm_schema\": \"omop\"}},\n        tools={\"my_package\": {\"extra\": {\"backend\": \"test_backend\"}}},\n    )\n    monkeypatch.setattr(\"my_package.module.load_stack_config\", lambda: cfg)\n    # ... test against the in-memory config\n</code></pre> <p>This covers the vast majority of tests. No file I/O, no environment-specific setup needed.</p>"},{"location":"integration/#integration-tests-dedicated-test-resource","title":"Integration tests: dedicated test resource","text":"<p>For tests that exercise a real database (e.g. PostgreSQL-specific SQL, bulk loading, trigger management), use a dedicated named resource in the user's config and never a profile override of the production resource.</p> <p>The canonical resource name is <code>test_&lt;package&gt;_db</code> (e.g. <code>test_cdm_db</code> for omop-alchemy). Keeping the name distinct from the production resource (<code>cdm_db</code>) is a mandatory safety guard: the test suite must never accidentally connect to a production database.</p> <p>In <code>conftest.py</code>, resolve the test resource explicitly:</p> <pre><code>_TEST_RESOURCE = \"test_cdm_db\"\n\n@pytest.fixture(scope=\"session\")\ndef pg_engine():\n    # CI: sets ENGINE_CDM env var no file I/O needed\n    url = os.getenv(\"ENGINE_CDM\")\n    if url:\n        return sa.create_engine(url, future=True)\n\n    # Local Dev:  read dedicated test resource from config\n    from oa_configurator import Resolver, load_stack_config\n    from oa_configurator.package_base import ConfigurationError\n    try:\n        stack = load_stack_config()\n        resolved = Resolver(stack).resolve_resource(_TEST_RESOURCE)\n        return resolved.create_engine()\n    except (FileNotFoundError, KeyError, ConfigurationError):\n        pytest.skip(\"No PostgreSQL test database configured.\")\n</code></pre> <p>Why not <code>OA_ACTIVE_PROFILE=test</code>? Setting <code>OA_ACTIVE_PROFILE</code> globally in <code>conftest.py</code> affects every test that calls <code>load_stack_config()</code>, including unit tests that monkeypatch it. A dedicated resource name scopes the real-DB resolution to only the fixture that needs it, and leaves <code>cdm_db</code> unambiguously pointing at production data throughout the test session.</p> <p>Add the test resource to <code>~/.config/omop/config.toml</code>:</p> <pre><code>[databases.pg_test]\ndialect       = \"postgresql+psycopg\"\nhost          = \"localhost\"\nport          = 5432\nuser          = \"test\"\npassword      = \"test\"\ndatabase_name = \"test_db\"\n\n[resources.test_cdm_db]\ndatabase   = \"pg_test\"\ncdm_schema = \"public\"\n</code></pre> <p>Safety: the test resource must point to a dedicated, empty database. If your test session drops and recreates schemas, add a runtime guard that compares the resolved URL of <code>test_cdm_db</code> against all other configured resources and calls <code>pytest.fail()</code> on any match.</p>"},{"location":"integration/#multiple-environments","title":"Multiple environments","text":""},{"location":"integration/#adding-a-second-resource-of-the-same-type","title":"Adding a second resource of the same type","text":"<p>The <code>omop-config configure</code> command creates one resource per semantic name by default (e.g. <code>cdm_db</code> for omop-alchemy). To add a second \u2014 for example a production CDM alongside a local development one \u2014 use <code>--resource-name</code>:</p> <pre><code>omop-config configure omop_alchemy --resource-name cdm_db_prod\n</code></pre> <p>This creates <code>cdm_db_prod</code> without touching the existing <code>cdm_db</code>. Because two resources now exist for the same package, configure automatically prompts you to choose the default at the end of the same run \u2014 no second invocation needed.</p> <p>To change the default later, edit <code>config.toml</code> directly:</p> <pre><code>[tools.omop_alchemy]\ndefault_resource = \"cdm_db_prod\"\n</code></pre>"},{"location":"integration/#when-does-default_resource-matter","title":"When does default_resource matter?","text":"<p><code>default_resource</code> is only consulted when you have multiple resources of the same type configured. With a single CDM resource and a single EMB resource, it is set automatically by configure and you never need to think about it.</p>"},{"location":"integration/#resource_aliases-a-naming-compatibility-tool","title":"resource_aliases \u2014 a naming compatibility tool","text":"<p><code>resource_aliases</code> is NOT for switching environments. It solves a different problem: if your resource is named differently from what packages expect (e.g. <code>my_hospital_cdm</code> instead of <code>cdm_db</code>), aliases let packages find it without renaming your resource:</p> <pre><code>[resource_aliases]\ncdm_db = \"my_hospital_cdm\"   # packages asking for cdm_db get my_hospital_cdm\n</code></pre> <p>For multi-environment use, create multiple named resources and use <code>default_resource</code> instead.</p>"},{"location":"integration/#docker-compose","title":"Docker Compose","text":""},{"location":"integration/#how-it-works","title":"How it works","text":"<p><code>~/.config/omop/config.toml</code> lives on the host (or in a container's home directory) and is the single source of truth.  Docker Compose is only needed to provide database credentials at container startup. The app itself always reads from the TOML file, never from environment variables at runtime.</p> <p>The workflow:</p> <ol> <li>A gitignored <code>.env</code> file holds secrets that Docker Compose substitutes into its YAML.</li> <li>The container's startup command calls <code>omop-config configure &lt;package&gt;</code> with <code>--flags</code>,    writing those values into <code>config.toml</code> once at startup.</li> <li>After that, the app reads <code>config.toml</code> normally without any environment variables involved.</li> </ol> <p>The <code>.env</code> file is a Docker Compose concern only. It is never loaded by the Python app.</p>"},{"location":"integration/#example","title":"Example","text":"<p><code>.env</code> (gitignored):</p> <pre><code>POSTGRES_USER=omop\nPOSTGRES_PASSWORD=secret\nPOSTGRES_DB=omop_cdm\n</code></pre> <p><code>docker-compose.yml</code>:</p> <pre><code>services:\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}\n      POSTGRES_DB: ${POSTGRES_DB}\n\n  app:\n    build: .\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}\n      POSTGRES_DB: ${POSTGRES_DB}\n    command: &gt;\n      bash -c \"\n        omop-config configure my_package\n          --database cdm --dialect postgresql+psycopg\n          --host db --port 5432\n          --user $$POSTGRES_USER --password $$POSTGRES_PASSWORD\n          --database-name $$POSTGRES_DB --cdm-schema omop &amp;&amp;\n        exec my_app_entrypoint\n      \"\n</code></pre> <p>$$VAR escaping</p> <p>Use <code>$$VAR</code> (double dollar) inside a <code>command:</code> string so Docker Compose passes the literal variable name to the shell rather than substituting it at YAML-parse time.</p> <p>If your stack has more than one package (e.g., <code>my_package</code> and <code>omop_alchemy</code>), add a separate <code>omop-config configure</code> call for each, chained with <code>&amp;&amp;</code>:</p> <pre><code>command: &gt;\n  bash -c \"\n    omop-config configure omop_alchemy\n      --database cdm --dialect postgresql+psycopg\n      --host db --port 5432\n      --user $$POSTGRES_USER --password $$POSTGRES_PASSWORD\n      --database-name $$POSTGRES_DB --cdm-schema omop &amp;&amp;\n    omop-config configure my_package\n      --database cdm --dialect postgresql+psycopg\n      --host db --port 5432\n      --user $$POSTGRES_USER --password $$POSTGRES_PASSWORD\n      --database-name $$POSTGRES_DB --cdm-schema omop &amp;&amp;\n    exec my_app_entrypoint\n  \"\n</code></pre> <p>Each call is scoped to its own package. The <code>--host</code> value for <code>omop_alchemy</code> configures the CDM database; the <code>--host</code> value for <code>my_package</code> configures that package's database.  No prefix is needed because the package name is the namespace.</p>"},{"location":"integration/#security-note","title":"Security note","text":"<p>The <code>config.toml</code> written by the container will contain the database password in plaintext. This is acceptable for local development containers.  Restrict the file permissions:</p> <pre><code>chmod 600 ~/.config/omop/config.toml\n</code></pre> <p><code>omop-config</code> will warn at load time if the file has looser permissions.</p>"},{"location":"integration/#toml-snippet-for-your-readme","title":"TOML snippet for your README","text":"<p>Add a Configuration section to your package README:</p> <pre><code>## Configuration\n\nRequires `omop-config` to be run once. See the\n[`oa-configurator` quickstart](link) for initial setup.\n\nAdd the following to `~/.config/omop/config.toml`:\n\n\\`\\`\\`toml\n[tools.my_package]\ndefault_resource = \"default\"\n\n[tools.my_package.extra]\nbackend   = \"default\"\ndata_path = \"/path/to/data\"\n\\`\\`\\`\n\nThen run:\n\\`\\`\\`bash\nomop-config configure my_package\n\\`\\`\\`\n</code></pre>"},{"location":"logging/","title":"Logging","text":"<p>This package provides a unified logging setup for the OMOP Python stack. All packages configure their loggers through the same call and formatter.</p>"},{"location":"logging/#basic-usage","title":"Basic usage","text":"<pre><code>from oa_configurator import configure_logging\n\n# WARNING level (default)\nconfigure_logging(extra_namespaces=[\"my_package\"])\n\n# From a loaded config file (uses [logging] block)\nfrom oa_configurator import load_stack_config\nconfigure_logging(load_stack_config(), extra_namespaces=[\"my_package\"])\n</code></pre> <p><code>configure_logging</code> is idempotent: it is safe to call multiple times with the same arguments.</p>"},{"location":"logging/#verbosity","title":"Verbosity","text":"<p>Log level is driven by the <code>verbosity</code> parameter, which maps directly to CLI <code>-v</code> count flags:</p> <code>verbosity</code> Level <code>0</code> (default) WARNING <code>1</code> (<code>-v</code>) INFO <code>2+</code> (<code>-vv</code>) DEBUG <pre><code>configure_logging(verbosity=1, extra_namespaces=[\"omop_graph\"])\n</code></pre>"},{"location":"logging/#format","title":"Format","text":"<p>All log records use a single fixed format:</p> <pre><code>2026-01-15 14:32:01 | my_package | INFO | Connected to database\n</code></pre> <p>Output always goes to stderr. There are no alternative handlers or JSON formatters -&gt; route stderr to a log aggregator if needed.</p>"},{"location":"logging/#extra_namespaces","title":"<code>extra_namespaces</code>","text":"<p><code>oa-configurator</code> never hardcodes downstream package names. Each consuming package passes its own namespace:</p> <pre><code>configure_logging(verbosity=1, extra_namespaces=[\"omop_graph\", \"omop_emb\"])\n</code></pre> <p>Both <code>oa_configurator</code> and all listed namespaces get the same level and handler.</p>"},{"location":"logging/#level-override-from-config-file","title":"Level override from config file","text":"<p>The <code>[logging]</code> section in <code>config.toml</code> can set a fixed level that overrides <code>verbosity</code>:</p> <pre><code>[logging]\nlevel = \"DEBUG\"\n\n[logging.loggers]\n\"sqlalchemy.engine\" = \"INFO\"\n\"httpx\" = \"WARNING\"\n</code></pre> <p><code>level</code> applies to all OMOP namespaces. <code>loggers</code> applies fine-grained overrides to any specific logger.</p>"},{"location":"logging/#get_logger","title":"<code>get_logger</code>","text":"<pre><code>from oa_configurator import get_logger\n\nlogger = get_logger(\"my_package.module\")\nlogger.info(\"Hello from my package\")\n</code></pre> <p>Thin wrapper around <code>logging.getLogger()</code> as a single import point for all packages.</p>"},{"location":"profiles/","title":"Profiles","text":"<p>Profiles let you maintain one config file for multiple environments (local dev, staging, prod, test) without duplicating connection blocks.</p>"},{"location":"profiles/#how-profiles-work","title":"How profiles work","text":"<p>A profile defines connections, resources, and/or tools that replace the base entries with the same name when that profile is active. Base entries not mentioned in the profile are used unchanged.</p> <pre><code># Base config\n[databases.cdm]\ndialect       = \"postgresql+psycopg\"\nhost          = \"prod.example.com\"\nport          = 5432\nuser          = \"omop\"\npassword      = \"prod_secret\"\ndatabase_name = \"omop_cdm\"\n\n[resources.default]\ndatabase   = \"cdm\"\ncdm_schema = \"omop\"\n\n# Test profile (only what changes)\n[profiles.test.databases.cdm]\ndialect       = \"postgresql+psycopg\"\nhost          = \"localhost\"\nport          = 5433\nuser          = \"test_user\"\npassword      = \"test_pass\"\ndatabase_name = \"omop_test\"\n\n[profiles.test.resources.default]\ndatabase   = \"cdm\"\ncdm_schema = \"test_omop\"\n</code></pre> <p>When the <code>test</code> profile is active, <code>resolver.resolve_database(\"cdm\")</code> returns the test host/database.</p>"},{"location":"profiles/#switching-profiles","title":"Switching profiles","text":"<p>Persistent: writes <code>active_profile</code> to the TOML file and re-exports <code>config.env</code>:</p> <pre><code>omop-config use test\nomop-config use local\n</code></pre> <p>Per-session: env var override, does not modify the file:</p> <pre><code>OA_ACTIVE_PROFILE=test omop-config doctor\nOA_ACTIVE_PROFILE=prod python my_script.py\n</code></pre>"},{"location":"profiles/#profiles-in-tests","title":"Profiles in tests","text":"<p>Package tests should use <code>StackConfig.for_session()</code> (no file I/O). For integration tests that need a real database:</p> <pre><code># conftest.py\nimport os\nimport pytest\n\n@pytest.fixture(autouse=True, scope=\"session\")\ndef use_test_profile():\n    os.environ[\"OA_ACTIVE_PROFILE\"] = \"test\"\n    yield\n    os.environ.pop(\"OA_ACTIVE_PROFILE\", None)\n</code></pre> <p>This requires a <code>[profiles.test]</code> section in <code>~/.config/omop/config.toml</code> pointing at the test database. Document this in your package's README.</p>"},{"location":"profiles/#profile-with-additional-connections","title":"Profile with additional connections","text":"<p>Profiles can introduce new connections not present in the base config:</p> <pre><code>[profiles.local_only.databases.local_vocab]\ndialect       = \"sqlite\"\ndatabase_name = \"/data/vocab.db\"\n\n[profiles.local_only.resources.default]\ndatabase       = \"cdm\"         # uses base cdm database\nvocab_database = \"local_vocab\"  # new database defined in this profile\ncdm_schema     = \"omop\"\n</code></pre> <p>Cross-references in profile resources are validated against both base and profile databases at load time.</p>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#1-install","title":"1. Install","text":"<pre><code>pip install oa-configurator\n</code></pre>"},{"location":"quickstart/#2-configure-your-first-package","title":"2. Configure your first package","text":"<p>After installing a package that supports oa_configurator, run its configure command:</p> <pre><code>omop-config configure &lt;package&gt;   # e.g. omop_alchemy, omop_emb\n</code></pre> <p>This prompts for any database connections and package-specific settings that package declares, then writes <code>~/.config/omop/config.toml</code>. Packages that own a database resource (such as a CDM database) will prompt for connection details and schema names as part of this step.</p> <p>To create an empty config file without prompts (useful for scripted setups), use:</p> <pre><code>omop-config init\n</code></pre> <p>Note: Passwords are stored in plaintext. Keep the file readable only by your user: <code>chmod 600 ~/.config/omop/config.toml</code>. Secret management improvements are planned for a future release.</p>"},{"location":"quickstart/#3-verify","title":"3. Verify","text":"<pre><code>omop-config show\n</code></pre> <p>Prints your config as JSON. Validation errors (unknown field, missing cross-reference) appear here.</p>"},{"location":"quickstart/#4-test-connectivity","title":"4. Test connectivity","text":"<pre><code>omop-config verify\n</code></pre> <p>Reports OK / FAIL for each configured connection, with latency.</p>"},{"location":"quickstart/#5-export-for-docker-compose","title":"5. Export for Docker Compose","text":"<pre><code>omop-config export-env\n</code></pre> <p>Writes <code>~/.config/omop/config.env</code>. Docker Compose services read it via <code>env_file:</code>.</p>"},{"location":"quickstart/#verbosity","title":"Verbosity","text":"<p>All <code>omop-config</code> commands accept <code>-v</code> / <code>-vv</code> to increase log output:</p> <pre><code>omop-config -v verify     # INFO level\nomop-config -vv show      # DEBUG level\n</code></pre>"},{"location":"quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Switch profiles: <code>omop-config use &lt;profile&gt;</code> (see Profiles)</li> <li>Configure a package interactively: <code>omop-config configure omop_alchemy</code> (see Integration)</li> <li>All TOML fields: Config reference</li> </ul>"},{"location":"secrets/","title":"Secrets","text":"<p>Not yet implemented.</p> <p>Secret management is planned for a future release.</p> <p>In the current version, passwords are stored as plaintext in <code>~/.config/omop/config.toml</code>. Restrict file permissions to limit exposure:</p> <pre><code>chmod 600 ~/.config/omop/config.toml\n</code></pre> <p><code>ResolvedDatabaseTarget.safe_url</code> redacts passwords in all display and log output. The plaintext <code>.url</code> is used only internally for engine creation.</p>"},{"location":"secrets/#planned-secret_source","title":"Planned: <code>secret_source</code>","text":"<p>A future <code>secret_source</code> field on <code>ConnectionConfig</code> will support indirect credential lookup, keeping passwords out of the TOML file entirely:</p> <pre><code>[databases.prod]\ndialect       = \"postgresql+psycopg\"\nhost          = \"prod.hospital.org\"\ndatabase_name = \"omop_cdm\"\nuser          = \"omop_prod\"\nsecret_source = \"env:PROD_DB_PASSWORD\"   # or \"file:/run/secrets/prod.password\"\n</code></pre> <p>Planned source formats:</p> Format Description <code>env:VARIABLE_NAME</code> Read from environment variable at resolution time <code>file:PATH</code> Read from file (absolute path or relative to config dir) Vault / cloud (under consideration) AWS Secrets Manager, GCP Secret Manager, Azure Key Vault"},{"location":"api/loader/","title":"Loader","text":"<p>The loader reads a TOML file, validates it against the typed models, and applies any environment-variable overrides before returning a bound <code>StackConfig</code>.</p> <p>Helpers for loading the shared stack configuration from TOML.</p>"},{"location":"api/loader/#oa_configurator.loader.load_stack_config","title":"load_stack_config","text":"<pre><code>load_stack_config() -&gt; StackConfig\n</code></pre> <p>Load a :class:<code>StackConfig</code> from <code>CONFIG_PATH</code> (default <code>~/.config/omop/config.toml</code>, overridable via <code>OA_CONFIG_PATH</code>).</p> <p>The active profile can be overridden via the <code>OA_ACTIVE_PROFILE</code> environment variable without modifying the file.</p>"},{"location":"api/loader/#oa_configurator.loader.load_stack_config--raises","title":"Raises","text":"<p>FileNotFoundError     If <code>CONFIG_PATH</code> does not exist.</p>"},{"location":"api/logging/","title":"Logging API","text":"<p>Logging configuration for the OMOP stack.</p>"},{"location":"api/logging/#oa_configurator.logging_config.LoggingConfig","title":"LoggingConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Logging overrides from the <code>[logging]</code> section of config.toml.</p> <p><code>level</code> overrides the verbosity-derived level for all OMOP loggers. <code>loggers</code> applies fine-grained level overrides to specific named loggers (e.g. <code>{\"sqlalchemy.engine\": \"INFO\"}</code>).</p>"},{"location":"api/logging/#oa_configurator.logging_config.RedactingFormatter","title":"RedactingFormatter","text":"<p>               Bases: <code>Formatter</code></p> <p>Logging formatter that redacts sensitive key=value pairs from log messages.</p>"},{"location":"api/logging/#oa_configurator.logging_config.configure_logging","title":"configure_logging","text":"<pre><code>configure_logging(\n    config: LoggingConfig | _HasLoggingConfig | None = None,\n    *,\n    verbosity: int = 0,\n    extra_namespaces: list[str] | None = None,\n    console: Any = None,\n) -&gt; None\n</code></pre> <p>Configure Python logging for the OMOP stack.</p> <p>Safe to call multiple times; re-applying the same arguments is idempotent.</p>"},{"location":"api/logging/#oa_configurator.logging_config.configure_logging--parameters","title":"Parameters","text":"<p>config:     A <code>LoggingConfig</code> or a <code>StackConfig</code> (<code>logging</code> block extracted     automatically). When <code>config.level</code> is set it takes precedence over     <code>verbosity</code>. verbosity:     Number of <code>-v</code> flags passed by the user. <code>0</code> \u2192 WARNING,     <code>1</code> \u2192 INFO, <code>2+</code> \u2192 DEBUG. extra_namespaces:     Additional logger namespaces to configure alongside <code>oa_configurator</code>.     Pass your package name here, e.g. <code>extra_namespaces=[\"omop_graph\"]</code>.</p>"},{"location":"api/logging/#oa_configurator.logging_config.get_logger","title":"get_logger","text":"<pre><code>get_logger(name: str) -&gt; logging.Logger\n</code></pre> <p>Return a named logger; single import point for consuming packages.</p>"},{"location":"api/models/","title":"Models","text":"<p>Typed Pydantic models that represent the human-managed configuration structure. All models are exported from the top-level <code>oa_configurator</code> package.</p>"},{"location":"api/models/#stackconfig","title":"StackConfig","text":"<p>Root configuration object. Also the entry point for programmatic construction via <code>StackConfig.for_session()</code>.</p> <p>               Bases: <code>BaseModel</code></p> <p>Root model for <code>~/.config/omop/config.toml</code>.</p> <p>Holds the entire OMOP stack configuration in one object: named database connections, logical resources, per-package tool sections, and environment profiles. Loaded from disk by :func:<code>~oa_configurator.loader.load_stack_config</code>; constructed in memory via :meth:<code>for_session</code> for tests and scripts (no file I/O).</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.loaded_path","title":"loaded_path  <code>property</code>","text":"<pre><code>loaded_path: Path | None\n</code></pre> <p>Path of the TOML file this config was loaded from, if any.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.bind_loaded_path","title":"bind_loaded_path","text":"<pre><code>bind_loaded_path(path: Path) -&gt; None\n</code></pre> <p>Record the path of the TOML file this config was loaded from.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.database_names","title":"database_names","text":"<pre><code>database_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured database names.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.for_session","title":"for_session  <code>classmethod</code>","text":"<pre><code>for_session(\n    *,\n    databases: dict[str, DatabaseConfig] | None = None,\n    resources: dict | None = None,\n    tools: dict | None = None,\n    profiles: dict | None = None,\n    active_profile: str | None = None,\n    resource_aliases: dict[str, str] | None = None,\n) -&gt; StackConfig\n</code></pre> <p>Build a config in memory without a TOML file.</p> <p>Intended for tests and scripts. Cross-references are validated at construction time, same as for file-loaded configs.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.profile_names","title":"profile_names","text":"<pre><code>profile_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured profile names.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.resource_names","title":"resource_names","text":"<pre><code>resource_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured resource names.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.tool_names","title":"tool_names","text":"<pre><code>tool_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured tool names.</p>"},{"location":"api/models/#oa_configurator.models.StackConfig.validate_references","title":"validate_references","text":"<pre><code>validate_references() -&gt; StackConfig\n</code></pre> <p>Ensure all named cross-references point at configured objects.</p>"},{"location":"api/models/#databaseconfig","title":"DatabaseConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Complete specification of one named database: server address, credentials, and target database.</p> <p>Referenced by :attr:<code>ResourceConfig.database</code> and :attr:<code>ResourceConfig.vocab_database</code>. Each entry under <code>[databases]</code> in <code>config.toml</code> maps to one instance of this model.</p> <p>Passwords are stored in plaintext for now; secret management support is planned for a future release.</p>"},{"location":"api/models/#oa_configurator.models.DatabaseConfig.build_url","title":"build_url","text":"<pre><code>build_url() -&gt; str\n</code></pre> <p>Build the full connection URL, including the plaintext password.</p>"},{"location":"api/models/#oa_configurator.models.DatabaseConfig.build_url--returns","title":"Returns","text":"<p>str     SQLAlchemy-compatible connection URL. For SQLite, returns     <code>sqlite:///&lt;database&gt;</code> (or <code>sqlite:///:memory:</code> when     <code>database</code> is not set).</p>"},{"location":"api/models/#oa_configurator.models.DatabaseConfig.safe_url","title":"safe_url","text":"<pre><code>safe_url() -&gt; str\n</code></pre> <p>Build the connection URL with the password redacted.</p> <p>Safe for logging and display. Identical to <code>build_url()</code> for SQLite connections, which carry no password.</p>"},{"location":"api/models/#oa_configurator.models.DatabaseConfig.safe_url--returns","title":"Returns","text":"<p>str     Connection URL with <code>***</code> substituted for the password field.</p>"},{"location":"api/models/#oa_configurator.models.DatabaseConfig.to_env_pairs","title":"to_env_pairs","text":"<pre><code>to_env_pairs(prefix: str) -&gt; list[str]\n</code></pre> <p>Return <code>PREFIX_FIELD=value</code> strings for each non-None field.</p> <p>Used by :func:<code>~oa_configurator.io.write_env_file</code> to emit env vars for Docker Compose <code>env_file:</code>. Field names are uppercased directly (e.g. <code>host</code> \u2192 <code>PREFIX_HOST</code>), so adding a new field here automatically appears in the export without touching <code>io.py</code>. Config-only flags (<code>read_only</code>, <code>test_only</code>) are excluded \u2014 they are not database connection parameters.</p>"},{"location":"api/models/#resourceconfig","title":"ResourceConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Maps the OMOP logical roles (CDM, vocab, results) to named databases and schema names.</p> <p>The unit that consuming packages configure once and reference by name. Most packages only need a single <code>cdm_db</code> resource. Each entry under <code>[resources]</code> in <code>config.toml</code> maps to one instance of this model.</p>"},{"location":"api/models/#toolconfig","title":"ToolConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-package section in <code>config.toml</code> (<code>[tools.&lt;name&gt;]</code>).</p> <p><code>default_resource</code> names which resource this package reads from. <code>extra</code> holds the package-specific typed fields declared on the package's :class:<code>~oa_configurator.PackageConfigBase</code> subclass.</p>"},{"location":"api/models/#profileoverrideconfig","title":"ProfileOverrideConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Named environment overlay (<code>[profiles.&lt;name&gt;]</code> in <code>config.toml</code>).</p> <p>Entries replace base entries with the same name when the profile is active. Anything not mentioned in the profile is inherited from the base config unchanged. Useful for switching between local-dev and production databases without editing the base config.</p>"},{"location":"api/package-base/","title":"PackageConfigBase API","text":"<p>               Bases: <code>BaseModel</code></p> <p>Typed view over a package's <code>[tools.&lt;tool_name&gt;]</code> TOML section.</p> <p>Subclass this and declare the class variables below. Users who name their resource differently can add a <code>[resource_aliases]</code> section to config.toml (e.g. <code>cdm_db = \"my_prod\"</code>) so all packages resolve correctly without per-package <code>default_resource</code> overrides.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase--attributes","title":"Attributes","text":"<p>tool_name : str     Key used in <code>[tools.&lt;name&gt;]</code>. Must be set on every subclass. required_resources : tuple[str, ...]     Canonical resource names this package depends on. A missing resource     at :meth:<code>from_stack</code> time raises :exc:<code>ConfigurationError</code>. owned_resources : tuple[ResourceSpec, ...]     Resources this package is responsible for configuring interactively.     <code>omop-config configure</code> prompts for these before package extras. test_resources : tuple[ResourceSpec, ...]     Optional test-only resources. <code>omop-config configure</code> presents a     Y/N prompt for these after the main resource flow. Marked with a     DROP SCHEMA warning; a collision check prevents pointing at any     already-configured non-test resource. extra_logging_namespaces : tuple[str, ...]     Logger namespaces of transitive dependencies to configure alongside     this package. The package's own <code>tool_name</code>     are always included -&gt; only list additional roots here, e.g.     <code>(\"&lt;my_extra_package_to_log\",)</code>. Missing namespaces are harmless.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.configure_logging","title":"configure_logging  <code>classmethod</code>","text":"<pre><code>configure_logging(\n    config=None, *, verbosity: int = 0, console=None\n) -&gt; None\n</code></pre> <p>Configure logging for this package and its declared transitive dependencies.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.from_stack","title":"from_stack  <code>classmethod</code>","text":"<pre><code>from_stack(config: StackConfig) -&gt; Self\n</code></pre> <p>Load this package's section from a :class:<code>StackConfig</code>.</p> <p>Validates that all :attr:<code>required_resources</code> (or the <code>default_resource</code> override) are present in the config before instantiating. Alias resolution via <code>config.resource_aliases</code> is applied before the existence check. Raises :exc:<code>ConfigurationError</code> with an actionable message if any are missing.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.get_config","title":"get_config  <code>classmethod</code>","text":"<pre><code>get_config() -&gt; Self\n</code></pre> <p>Load this package's config from the active stack config file.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.get_engine","title":"get_engine  <code>classmethod</code>","text":"<pre><code>get_engine(\n    resource_name: str | None = None, **engine_kwargs: Any\n) -&gt; Any\n</code></pre> <p>Create a SQLAlchemy engine for a resource.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.get_engine--parameters","title":"Parameters","text":"<p>resource_name:     Resource to resolve. If <code>None</code>, uses <code>tool.default_resource</code>     from the config file, or falls back to <code>required_resources[0]</code>. **engine_kwargs:     Forwarded to :meth:<code>~oa_configurator.resolver.ResolvedResource.create_engine</code>.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.get_engine--raises","title":"Raises","text":"<p>ConfigurationError     If no resource name can be determined.</p>"},{"location":"api/package-base/#oa_configurator.package_base.PackageConfigBase.to_extra_dict","title":"to_extra_dict","text":"<pre><code>to_extra_dict() -&gt; dict[str, Any]\n</code></pre> <p>Serialize back to the dict stored in <code>ToolConfig.extra</code>.</p>"},{"location":"api/resolver/","title":"Resolver","text":"<p>The <code>Resolver</code> turns logical names in a <code>StackConfig</code> into concrete typed handles with secrets resolved and paths expanded.</p>"},{"location":"api/resolver/#resolver_1","title":"Resolver","text":"<p>Resolves logical names in a StackConfig into typed, usable handles.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.active_profile_name","title":"active_profile_name","text":"<pre><code>active_profile_name() -&gt; str | None\n</code></pre> <p>Return the name of the currently active profile, or None.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.database_names","title":"database_names","text":"<pre><code>database_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured database names.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.effective_database","title":"effective_database","text":"<pre><code>effective_database(name: str) -&gt; DatabaseConfig\n</code></pre> <p>Return the active DatabaseConfig for a database name.</p> <p>Profile overlay databases take precedence over base databases. Unlike <code>resolve_database</code>, this returns the raw config object rather than a resolved target; useful when individual fields (host, port, etc.) are needed directly.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.effective_database--parameters","title":"Parameters","text":"<p>name : str     Database name as it appears in <code>[databases]</code> or a profile overlay.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.effective_database--returns","title":"Returns","text":"<p>DatabaseConfig     The effective database config for the active profile.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.effective_database--raises","title":"Raises","text":"<p>KeyError     If name does not exist in the base config or the active profile.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.from_active_config","title":"from_active_config  <code>classmethod</code>","text":"<pre><code>from_active_config() -&gt; Resolver\n</code></pre> <p>Create a Resolver from the currently active stack config file.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.profile_names","title":"profile_names","text":"<pre><code>profile_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured profile names.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_database","title":"resolve_database","text":"<pre><code>resolve_database(name: str) -&gt; ResolvedDatabaseTarget\n</code></pre> <p>Resolve a database name to a concrete target.</p> <p>Profile overlay databases take precedence over base databases.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_resource","title":"resolve_resource","text":"<pre><code>resolve_resource(name: str) -&gt; ResolvedResource\n</code></pre> <p>Resolve a resource name to a concrete bundle of DB targets and schemas.</p> <p>Applies profile overlays and resource aliases. The vocab DB falls back to the primary DB when not explicitly configured; the vocab schema falls back to the CDM schema under the same condition.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_resource--parameters","title":"Parameters","text":"<p>name : str     Resource name or alias as declared in <code>[resources]</code> or     <code>[resource_aliases]</code>.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_resource--returns","title":"Returns","text":"<p>ResolvedResource     Fully resolved resource with concrete database targets and     effective schema names.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_resource--raises","title":"Raises","text":"<p>KeyError     If name (after alias resolution) does not exist in the config.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_tool","title":"resolve_tool","text":"<pre><code>resolve_tool(name: str) -&gt; ResolvedToolConfig\n</code></pre> <p>Resolve a tool name to its configuration.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_tool--parameters","title":"Parameters","text":"<p>name : str     Tool name as declared in <code>[tools]</code>.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_tool--returns","title":"Returns","text":"<p>ResolvedToolConfig     Resolved config with the raw extra dict intact for consumption     by <code>PackageConfigBase.from_stack</code>.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resolve_tool--raises","title":"Raises","text":"<p>KeyError     If name does not exist in the config.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.resource_names","title":"resource_names","text":"<pre><code>resource_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured resource names.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.tool_names","title":"tool_names","text":"<pre><code>tool_names() -&gt; tuple[str, ...]\n</code></pre> <p>Return a sorted tuple of configured tool names.</p>"},{"location":"api/resolver/#oa_configurator.resolver.Resolver.with_overrides","title":"with_overrides","text":"<pre><code>with_overrides(\n    *,\n    databases: dict[str, DatabaseConfig] | None = None,\n    resources: dict[str, ResourceConfig] | None = None,\n    tools: dict[str, ToolConfig] | None = None,\n) -&gt; Resolver\n</code></pre> <p>Return a new Resolver with entries merged over the current config.</p> <p>Useful for session-level overrides without touching the TOML file.</p>"},{"location":"api/resolver/#resolved-types","title":"Resolved types","text":""},{"location":"api/resolver/#resolvedresource","title":"ResolvedResource","text":"<p>Resolved logical resource with concrete DB targets and effective schema names.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource--attributes","title":"Attributes","text":"<p>name : str     Logical name of the resource as declared in the config or an alias. database : ResolvedDatabaseTarget     Resolved primary database target for this resource. vocab_database : ResolvedDatabaseTarget     Resolved vocabulary database target for this resource. May be the same as     database if no separate vocab database is configured. cdm_schema : str     Effective CDM schema name for this resource. vocab_schema : str     Effective vocabulary schema name for this resource. May be the same as     cdm_schema if no separate vocab schema is configured. results_schema : str | None     Effective results schema name for this resource, or None if not configured.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.create_engine","title":"create_engine","text":"<pre><code>create_engine(\n    role: Literal[\"primary\", \"vocab\"] = \"primary\",\n    *,\n    execution_options: dict[str, Any] | None = None,\n    **kwargs: Any,\n) -&gt; Engine\n</code></pre> <p>Create a SQLAlchemy engine with the schema translate map applied.</p> <p>The schema translate map routes OMOP ORM models to the correct schemas automatically (<code>None</code> -&gt; cdm_schema, <code>\"vocab\"</code> -&gt; vocab_schema, <code>\"results\"</code> -&gt; results_schema when configured).</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.create_engine--parameters","title":"Parameters","text":"<p>role : {\"primary\", \"vocab\"}, optional     Which database target to create an engine for. Defaults to     <code>\"primary\"</code>. execution_options : dict, optional     Additional execution options merged into the engine. The     <code>schema_translate_map</code> key is set automatically and must     not be supplied here. **kwargs     Forwarded to <code>sqlalchemy.create_engine</code>.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.create_engine--returns","title":"Returns","text":"<p>sqlalchemy.engine.Engine     Engine configured with <code>schema_translate_map</code> for OMOP ORM routing.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.database_target","title":"database_target","text":"<pre><code>database_target(\n    role: Literal[\"primary\", \"vocab\"] = \"primary\",\n) -&gt; ResolvedDatabaseTarget\n</code></pre> <p>Return the resolved database target for a given role.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.database_target--parameters","title":"Parameters","text":"<p>role : {\"primary\", \"vocab\"}, optional     Which database target to return. Defaults to <code>\"primary\"</code>.     When <code>vocab_database</code> was not configured, <code>\"vocab\"</code> returns the     same target as <code>\"primary\"</code>.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.database_target--returns","title":"Returns","text":"<p>ResolvedDatabaseTarget     The concrete database target for role.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.database_target--raises","title":"Raises","text":"<p>ValueError     If role is not <code>\"primary\"</code> or <code>\"vocab\"</code>.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedResource.schema_translate_map","title":"schema_translate_map","text":"<pre><code>schema_translate_map() -&gt; dict[str | None, str | None]\n</code></pre> <p>SQLAlchemy schema translate map for OMOP ORM models.</p> Maps <p>None      \u2192 cdm_schema  (default / unqualified tables \u2192 CDM) \"vocab\"   \u2192 vocab_schema (or cdm_schema as fallback) \"results\" \u2192 results_schema (omitted when not configured)</p>"},{"location":"api/resolver/#resolveddatabasetarget","title":"ResolvedDatabaseTarget","text":"<p>Concrete database connection ready for engine creation.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedDatabaseTarget--attributes","title":"Attributes","text":"<p>name : str     Logical name of the connection as declared in the config. url : str     Full database URL including credentials.     TODO: make this a private attribute to avoid accidental password exposure;     requires a factory method or <code>__post_init__</code> since dataclass field     visibility can't be changed without breaking callers. safe_url : str     Database URL with credentials redacted, safe for logging and display.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedDatabaseTarget.create_engine","title":"create_engine","text":"<pre><code>create_engine(**kwargs: Any) -&gt; Engine\n</code></pre> <p>Create a SQLAlchemy engine for this connection.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedDatabaseTarget.create_engine--parameters","title":"Parameters","text":"<p>**kwargs     Forwarded to <code>sqlalchemy.create_engine</code>. The <code>read_only</code>     keyword is silently removed for SQLite connections, which do     not support it.</p>"},{"location":"api/resolver/#oa_configurator.resolver.ResolvedDatabaseTarget.create_engine--returns","title":"Returns","text":"<p>sqlalchemy.engine.Engine</p>"},{"location":"api/resolver/#resolvedtoolconfig","title":"ResolvedToolConfig","text":"<p>Resolved tool section with raw extra dict for PackageConfigBase consumption.</p>"}]}